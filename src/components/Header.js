//import React from 'react' (this is no longer necessary) only need to import if using class components

const Header = () => { //basic building block of a component- const and return- then build what you want
    return (
        <h5>My first functional component</h5>
    )
};

export default Header;